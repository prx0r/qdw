# Federation contractors

Register these through QDW's existing immutable ContractorRegistry. They do not create a second worker system.

`federation.trust-redteam` must be independent from the producer that wrote the integration patch.
